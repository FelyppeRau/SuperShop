@model SuperShop.Models.ProductViewModel  

@{
    ViewData["Title"] = "Create New Product";
}

<h1>New Product</h1>


<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" enctype="multipart/form-data"> @*enctype="multipart/form-data" informado depois do ProductViewModel*@
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            @*ALTERADO APÓS A CRIAÇÃO DA VIEW PARCIAL*@
            <partial name="_Products"/>

            <div class="form-group">
                <button type="submit" class="btn btn-primary"><i class="fa fa-save"></i> Create</button>
                @*<input type="submit" value="Create" class="btn btn-primary" />*@  @*RETIRADO QUANDO INCLUIMOS O <button></button> ACIMA - LINHA 20*@
                <a asp-action="Index" class="btn btn-success"><i class="fa fa-chevron-left"></i> Back to List</a>
            </div>
        </form>
    </div>
</div>



@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
